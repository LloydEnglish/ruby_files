<br>
<h1> List of users</h1>
<br>

<% @users.each do |user| %>
<p> <%= user.username %> <%= user.email %> <%= link_to "User Profile", user_path(user)%> or you can<%= link_to "User Edit", edit_user_path(user)%> or you can <%= link_to "Remove user", user_path(user), method: :delete %></p>
<% end %>

<br>
<br>
<p>Here's the path to creating new users <%= link_to "New User path", new_user_path()%> </p>
<br>
<p>Find me in app/views/user/index.html.erb</p>
